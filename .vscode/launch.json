{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb",
            "type": "lldb",
            "preLaunchTask": "build",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}